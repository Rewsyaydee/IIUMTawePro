name: Notify — Session Reminders

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger notification check
        run: curl -s -o /dev/null -w "%{http_code}" https://iium-tawe-pro.vercel.app/api/cron/notifications
